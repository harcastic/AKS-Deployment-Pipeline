# frontend-service.yml

apiVersion: v1
kind: Service

metadata:
  namespace: cloudgallery
  name: frontend-service

spec:
  selector:
    app: frontend

  ports:
  - port: 80
    targetPort: 3000

  type: ClusterIP